task 4 -- Step 5 default-arm byte-identity proof

Ruling R4: the control is the BRANCH HEAD BEFORE THIS TASK (263538df, task 3's
commit), reached with `git stash`, not master.  `git stash` cannot revert
committed work, so a stash-based comparison can only ever answer "did MY
uncommitted change move the default arm".  The against-master control is the
later 72-cell corpus gate.

method: the DSP blueprint string is `BLUEPRINT:<header fields>,"<base64
        payload>"<MD5>`.  Header field 8 is a wall-clock .NET tick stamped at
        emit time (codec.py:376) and the trailing MD5 covers it, so two runs of
        the SAME tree differ in exactly those bytes.  What is compared is the
        base64 LAYOUT PAYLOAD -- the entire placement -- plus the header with
        field 8 masked.

command: uv run flab2bp "$URL" --strategy freeform --budget 30

================================================================
URL A (the one task 3 used, so the two proofs are comparable)
  https://factoriolab.github.io/dsp/list?o=iron-ingot*60&ibe=conveyor-belt-2&mmr=arc-smelter~assembling-machine-2~chemical-plant~matrix-lab&v=11
  1 machine, 63 tiles

base (263538df, via git stash)
  file sha256    : bef2d2ef21560f0d8144c0b87dc8cd98518f747cd05ef78eef23885a6ca6c3d7
  layout payload : 690d3fc033d6066ef9497db542d96b5e7f8345870e93ea0e59cdb17f77b470a8
  header ts      : 639244018838976768
  trailing md5   : 9879FC743800653E2435BD54C629F599

head (task 4 working tree)
  file sha256    : 98f2baae282b4aa1d1dba7a8f601d848352efae39a5ff53c2af82901865b4387
  layout payload : 690d3fc033d6066ef9497db542d96b5e7f8345870e93ea0e59cdb17f77b470a8
  header ts      : 639244018711210752
  trailing md5   : 7DDC156974E79617F4B4E74CB3CB8087

head2 (task 4 working tree, second run -- the clock control)
  file sha256    : dd670e54b4e0e1c0676cac8301528f8949125753619cc66758f4bcd7210be1ee
  layout payload : 690d3fc033d6066ef9497db542d96b5e7f8345870e93ea0e59cdb17f77b470a8
  header ts      : 639244018920437376
  trailing md5   : E3FAC1CA579F1A2F286E5FE39487293F

  base payload == head payload           : True
  head payload == head2 payload          : True
  base header (minus timestamp) == head  : True
  three timestamps, three different values, same tree included -- which is
  what proves the differing bytes are the clock and not this change.

  NOTE: this payload hash 690d3fc0... is the SAME value task 3's Step 7
  evidence recorded.  The default arm has therefore not moved across task 3
  AND task 4, not merely across task 4.

================================================================
URL B (added by task 4: URL A is a one-machine build and is a weak control)
  https://factoriolab.github.io/dsp/list?o=titanium-glass*60&ibe=conveyor-belt-2&mmr=arc-smelter~assembling-machine-2~chemical-plant~matrix-lab&v=11

  head run 1 payload : 69abe119e9690fe366c38dcb14c1d77b1f027706ad30f077663ecc32e59acedc
  head run 2 payload : 69abe119e9690fe366c38dcb14c1d77b1f027706ad30f077663ecc32e59acedc
  base     payload   : 69abe119e9690fe366c38dcb14c1d77b1f027706ad30f077663ecc32e59acedc

  The two head runs were taken FIRST, to establish that this URL is
  run-to-run deterministic at --budget 30 before the comparison was allowed
  to mean anything.

================================================================
VERDICT

  BYTE IDENTICAL on both URLs (layout payload; the header timestamp is
  wall-clock by design and differs between two runs of one tree).

  This is the expected result: the change is a footprint erosion that is a
  no-op at width == height == 1, and `_Canvas.fits(x, y, 1, 1)` is the
  single-cell test it replaces, tile for tile.

================================================================
FIX ROUND 1 -- re-run after the review's Important 1 and 2

Both amendments are claimed byte-identical on the DEFAULT arm BY CONSTRUCTION,
and the reviewer is right that these two capture URLs cannot prove it: decoded,
their only power nodes are the Tesla Towers the planner itself places, which do
not exist in `canvas.buildings` when `_power_plan` runs --

  URL A  items {2002: 10, 2302: 1, 2011: 2, 2201: 1}   power nodes {2201: 1}
  URL B  items {2002: 97, 2302: 4, 2011: 20, 2304: 3, 2201: 3}  power nodes {2201: 3}

-- so the existing-power-node keepout loop finds zero peers and never punches a
halo into `free`.  The captures below are a regression check, not the argument.

The argument is structural:

  Important 1 (openness): the statement is now TEXTUALLY IDENTICAL to
    263538df.  `git diff -U0 263538df -- src/flab2bp/layout/freeform.py` no
    longer contains the `openness[...] += free[...]` statement at all; only
    added comments surround it.  Identity, not measurement.

  Important 2 (site-vs-site overlap): the added clearing is
    `free[gx - (w-1) : gx + w, gy - (h-1) : gy + h] = False`.  At w = h = 1
    that slice is exactly `free[gx:gx+1, gy:gy+1]` -- the single anchor cell.
    The line above it clears the power-node spacing stamp, and offset (0, 0)
    is one of its 21 cells:
      rules.power_node_keepout_offsets(tesla.power_node, tesla.power_node)
        -> 21 offsets at dz == 0, reach 2, (0, 0) present: True
    So on the default arm the new statement writes False over a cell that is
    already False.  A no-op, by construction.

capture (regression check, both URLs, same method as above):

  URL A  round-1 head payload 690d3fc033d6066ef9497db542d96b5e7f8345870e93ea0e59cdb17f77b470a8
         task-4  base payload 690d3fc033d6066ef9497db542d96b5e7f8345870e93ea0e59cdb17f77b470a8
  URL B  round-1 head payload 69abe119e9690fe366c38dcb14c1d77b1f027706ad30f077663ecc32e59acedc
         task-4  base payload 69abe119e9690fe366c38dcb14c1d77b1f027706ad30f077663ecc32e59acedc

VERDICT: BYTE IDENTICAL, still, against 263538df on both URLs.
